name: COTAHIST - teste executor normalizacao 2011-2026 V5

# V5 mantida apenas para execucao manual.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Teste
        run: |
          echo "WORKFLOW_OK"
          echo "NORMALIZACAO_V5_ATIVA"
